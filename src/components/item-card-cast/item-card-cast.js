import React from 'react';
import ItemList from '../itemlist/itemlist';
import { makeStyles } from '@material-ui/core/styles';

const ItemCardCast = (props) => {

    return(
        <ItemList items={null} page={'cast'} />
    )
}

export default ItemCardCast;